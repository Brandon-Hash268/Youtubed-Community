import { Form, Input, View } from "tamagui";

export function AddPost(params) {
    return(
        <View>
            <Form>
                <Input
                
                />
            </Form>
        </View>
    )
}